interface:
  display_name: "gstack-office-hours"
  short_description: "Studio greenlight for games — two modes. Studio mode: six forcing questions that expose player fantasy, reference..."
  default_prompt: "Use gstack-office-hours for this task."
policy:
  allow_implicit_invocation: true
